# steppY UI 원칙

402 × 874 한 화면에서 손가락으로 읽는 앱이다. 여기 적힌 것은 취향이 아니라
그 조건에서 반복해서 부딪힌 문제와, 그때마다 내린 결정이다. 새 화면을 만들거나
기존 화면을 고칠 때 이 기준으로 판단한다.

---

## 1. 세로 길이가 가장 비싸다

좁은 화면에서 세로로 길어지면 사용자는 **읽지 않고 스크롤**한다. 그래서 길이를
늘리는 모든 것 — 줄바꿈, 카드, 여백, 설명문 — 은 값을 치르고 들어와야 한다.

### 줄바꿈부터 없앤다

한 줄에 들어갈 수 있는 것이 두 줄이 되면, 화면에 담기는 정보가 절반으로 준다.

- **제목이 접히면 폭을 넓힐 자리를 먼저 찾는다.** 학교 상세의 긴 이름이 석 줄로
  접혔을 때, 글자 크기를 줄이는 대신 오른쪽 버튼(닫기·찜하기)을 흐름 밖으로 빼고
  `float` 한 칸만 남겼다. 첫 줄만 버튼을 비켜 가고 둘째 줄부터 폭을 다 쓴다 →
  석 줄이 두 줄이 됐다.
- **같은 층위의 값은 한 줄에 둔다.** `지원 가능 / 모집 / GPA 컷 / TOEFL 컷` 이
  두 줄로 나뉘면 앞 두 개만 읽고 만다. 배지 좌우 여백(16 → 9px)과 간격(10 → 6px)을
  줄여 넷을 한 줄에 넣었다. 그래도 넘치면 접지 말고 가로로 밀어서 본다
  (`flex-wrap: nowrap; overflow-x: auto`).
- **문장형 제목은 크기를 재서 정한다.** 데스크탑 기준 32px(`--fs-h1`)로 쓴
  랜딩 제목이 402px 화면에서 네 줄로 접혔다. `<br>` 로 의도한 자리 말고 엉뚱한
  데서 끊기면 문장이 아니라 단어 뭉치로 읽힌다.
  → **쓴 대로 들어가는 가장 큰 크기를 실제로 재서 고른다.**
  "교환학생 준비부터 추억 정리까지, / 교환학생의 모든 STEP을 도와줍니다" 는
  340px 폭에서 24px→2줄, 25px→3줄이라 24px(`1.5rem`)로 정했다.
  짧은 제목("Four Steps!")은 같은 크기로 한 줄이니 크기를 나눌 필요가 없다.
  **문구를 고치면 줄 수가 달라지니 다시 잰다.**
- **폭부터 확인하고 크기를 건드린다.** 위 제목이 접힌 진짜 이유는 글자가 커서가
  아니라 좌우 여백이 62px씩이라 본문 폭이 278px밖에 안 됐기 때문이다.
  앱 화면 기준(31px)으로 좁히니 340px이 되면서 접힘이 절반으로 줄었다.
- **긴 데이터를 넣기 전에 칸을 세어 본다.** 날씨 줄에 `비 오는 날 월 13일 · 67mm`
  를 넣었더니 한 줄이 69px로 부풀었다. `☔ 13일 · 67mm` 로 줄이고 "비 오는 날 월"
  이라는 설명은 목록 아래 캡션 한 줄로 옮겨서 52px이 됐다.

### 가로 폭이 남으면 옆으로 나란히 둔다

세로로 길어지는 가장 흔한 이유는 **폭이 남는데도 한 줄에 하나씩 쌓는 것**이다.
그룹을 세로로 나열하면 "정리된 목록"처럼 보이지만, 사용자에게는 두 번 스크롤이다.

- 검색 필터가 그랬다. 열 개 그룹을 한 줄에 하나씩 쌓아 시트가 화면 밖으로
  넘쳤다. 칩 두세 개짜리 그룹(국가·QS 순위 / 지원 자격·수업 언어 /
  토글 두 개)은 폭이 절반이면 충분하다 → `grid-template-columns: 1fr 1fr` 로
  깔고, 칩이 네 개 이상인 그룹(지역·기후·치안)만 `grid-column: 1 / -1` 로
  두 칸을 쓴다. **스크롤이 아예 없어졌다.**
- 같이 줄일 것: 칩 높이(34 → 30px)와 좌우 여백(16 → 11px), 그룹 간격
  (`--space-5` → `--space-3`). 다만 **누를 수 있는 최소치 아래로는 내리지
  않는다** — 30px이 손가락으로 안정적으로 눌리는 하한이다.
- 두 칸으로 나눈 뒤에는 **각 칸에서 줄바꿈이 새로 생겼는지 다시 본다.** 폭이
  반이 되니 `영어 수업 / 현지어 수업` 이 두 줄로 접혔다. 그룹 제목("수업 언어")이
  이미 문맥을 주므로 칩은 `영어 / 현지어` 로 줄였다 → 한 줄.
- 다 고친 뒤 `scrollHeight - clientHeight` 로 실제로 넘치는지 잰다. 0이어야 한다.

### 카드는 내용이 상자를 요구할 때만

껍데기(배경·테두리·그림자·안쪽 여백)는 그 자체로 세로 길이를 먹는다.
**내용보다 껍데기가 크면 껍데기를 벗긴다.**

- 인트로의 "네 걸음"에서 흰 카드를 걷어냈다. 네 개의 상자가 아니라 이어지는
  하나의 흐름이라, 카드로 끊으면 순서가 안 읽힌다. 여백과 번호로만 나눈다.
- 국제처 링크는 링크 한 줄이 전부였다. 카드를 지우고 한 줄로 뒀다.
- 확정 학교 카드 안의 출국 D-day도 카드였다. 카드 안에 카드가 들어앉아 "잘린
  상자"로 보여서, 껍데기만 벗기고 경로 그림은 남겼다(182 → 62px).
- 인트로 숫자(271/76/32)의 유리 카드도 없앴다. 가는 세로선으로만 나눈다.

### 여백은 두 번 겹치기 쉽다

컨테이너의 `gap` 과 자식의 `margin` 이 더해지는 실수가 반복됐다.

- 생활 점수의 근거 한 줄: `.score-rows` 의 `gap:16px` 위에 `margin-bottom:16px`
  이 얹혀 32px이었다. 아래 여백을 빼서 16px로 줄이고, 위는 당겨서(-8px) 자기
  지표에 붙였다. **설명은 설명하는 대상에 붙어 있어야 한다.**
- 여백을 고칠 때는 눈으로 보지 말고 `getBoundingClientRect()` 로 실제 간격을 잰다.

### 접을 수 있으면 접는다

- 생활 준비 4개(보험·장학금·통신사·계좌)는 제목만 세우고 눌러야 열린다.
  보험을 알아보는 날과 계좌를 여는 날은 다르다.
- 이미 체크리스트가 같은 방식이라 조작이 낯설지 않다 — **새 상호작용을 만들기
  전에 이미 쓰는 방식이 있는지 본다.**

---

## 2. 문구 다이어트

한국어 UI는 조사와 서술어로 금방 길어진다. **뜻이 유지되는 한 짧은 쪽**을 쓴다.

### 없애도 되는 단어를 찾는다

| 전 | 후 | 이유 |
| --- | --- | --- |
| 오늘의 할 일 | 할 일 | 오늘 것만 있는 게 아니다 |
| ② 날씨 · 생활 정보 | Weather of 미국 · Bellingham! | 번호와 분류어 대신 그 학교 이야기 |
| ⑤ 공식 링크 | 국제처 링크 | "공식"은 링크면 당연하다 |
| D-346 출국까지 | D-346 | 서울 → Liverpool 이 바로 위에 있다 |
| 비 오는 날 월 13일 | ☔ 13일 | 기호가 단어를 대신한다 |

단위는 숫자보다 작게 둔다. `271개` 에서 숫자는 36px, `개` 는 라벨과 같은 11px —
읽어야 하는 것은 숫자지 단위가 아니다.

### 맞춤법

문구를 짧게 쓰다 보면 조사와 보조용언을 붙였다 뗐다 하게 된다. 한 화면 안에서
흔들리면 짧아진 것보다 어색한 게 먼저 보인다. **맞고 틀림보다 한 가지로
통일하는 쪽**이 중요한 항목들이다.

**보조용언** — 한글맞춤법 47항은 띄어 씀을 원칙으로 하되 붙여 씀도 허용한다.
둘 다 맞으니 쓰임으로 가른다.

| 꼴 | 쓰는 법 | 왜 |
| --- | --- | --- |
| `-아/어 주세요` | **띄운다** — 입력해 주세요 | 사용자에게 시키는 말이라 동사가 또렷해야 한다 |
| `-아/어 드려요` | **붙인다** — 보여드려요 | 서비스가 하는 일. 한 덩어리로 읽힌다 |
| `-아/어 보세요` | **붙인다** — 기록해보세요 | 권유. 위와 같다 |

**의존명사는 띄운다.** `준비 중`, `할 수 있어요`, `그 밖에`, `한 것`.
`준비중` 처럼 붙이면 틀린다. 반대로 `한눈에`, `며칠` 은 한 단어라 붙인다.

**`예요` / `이에요`** — 앞말이 모음으로 끝나면 `예요`(점수**예요**, 추정치**예요**),
자음으로 끝나면 `이에요`(기준**이에요**, 뜻**이에요**).

**`돼` / `되`** — `되어`로 바꿔 말이 되면 `돼`. 저장**돼**요, 거부**됐**어요.

**이중 피동·한자 투를 쓰지 않는다.** `덮어써져요` → `바뀌어요`,
`원칙적으로 불가해요` → `원칙적으로 바꿀 수 없어요`. 짧아지기까지 한다.

**같은 말은 한 가지 표기로.** `학점 인정` 과 `학점인정` 이 같은 화면에 있었다.
새 문구를 쓰기 전에 그 말이 이미 어떻게 적혀 있는지 찾아본다.

**말투는 `-요` 체로 통일한다.** `-습니다` 는 딱딱하고, 한 문단 안에서 섞이면
(`정리돼 들어갑니다. 날짜와 사진도 함께 묶여요.`) 번역문처럼 읽힌다.
예외는 [privacy.html](privacy.html) — 법적 고지라 문서 전체가 `-습니다` 체다.

### 같은 말을 두 번 하지 않는다

- 지도 카드 제목이 `미국 · Bellingham` 인데 그 아래 캡션도 같은 말이었다 → 캡션 삭제.
- 날씨의 계절별 줄이 "서울보다 2°C 추움"을 계산해 주는데, DB의
  `korea_comparison` 도 "봄 서울보다 2도 따뜻" 이었다 → 같은 형태면 감춘다.
- `city` 칸에 나라 이름이 들어 있는 행이 있다(홍콩 `香港 Hong Kong`) →
  "홍콩 · 香港 Hong Kong" 이 되지 않게 도시가 나라를 품고 있으면 나라만 남긴다.
- 서울과의 강수 비교를 계절마다 붙이면 네 번 반복된다 → 목록 아래 한 줄로 모은다.

### 설명이 필요하면, 설명이 필요 없게 고칠 수 있는지 먼저 본다

`막대는 파견교 271곳끼리 견준 상대 위치예요. 그 아래 한 줄이 그 판단의 근거가 된
실제 수치고요.` — 이 문장은 읽고 나서도 무슨 말인지 모르겠다는 말을 들었다.

→ `막대가 길수록 다른 파견교보다 좋다는 뜻이에요. 막대 아래 한 줄은 그 막대가
나온 실제 숫자고요.`

**추상어("상대 위치", "판단의 근거")를 동사와 비교로 바꾼다.**

### 말투

- 화면 제목은 가볍게 말을 걸어도 된다: `How is 미국 · Bellingham?`,
  `Cost of living, 얼마나 들까?`
- 다만 **되돌릴 수 없는 동작은 가볍게 쓰지 않는다.** 회원 탈퇴는
  "계정을 지우면 되돌릴 수 없어요" 로 시작하고 무엇이 사라지는지 항목으로 적는다.

---

## 3. 숫자는 해석까지 준다

0~100 점수만 던지면 그 동네가 어떤 곳인지 알 수 없다. **점수 아래에 그 점수를
만든 실제 수치를 붙인다.**

```
🚌 교통 이동성  ████████░░  매우 우수
   도심까지 대중교통 36분 · 1km 안 정류장 50곳
```

- 비교 기준이 있으면 같이 준다. 날씨는 서울 평년값(기상청 1991~2020) 대비 차이로
  적고, 강수는 서울의 계절별 강수일수를 아래에 한 줄로 둔다.
- **기준이 없으면 만들어 내지 않는다.** 치안·상권 점수는 데이터셋에 서울이 없어
  서울과 견줄 수 없다. 그래서 271곳 안에서의 상대 위치로만 말한다.
- 근거가 없는 항목은 "자료 없음" 으로 비워 둔다. 반쪽짜리 숫자보다 낫다.

### 측정하지 않은 것을 단정하지 않는다

기후 필터 라벨이 `온화·잦은 비`, `고온다습`, `한랭·일조 짧음` 이었는데, 분류는
계절 평균기온 네 개로만 했고 강수·습도·일조 자료가 아예 없었다. 마드리드(여름
14mm·3일)와 리버풀(여름 92mm·17일)이 같은 칸에 들어가 있었다.

→ `겨울 온화` / `연중 더움` / `겨울 영하` 로 바꾸고, 강수는 실제로 수집해서
   따로 보여준다.

---

## 4. 값을 먼저 보여주고 비용은 나중에 받는다

> 이 장의 뼈대는 토스 제품 원칙(Toss Tech / Tossfeed 공개 자료)에서 가져와
> steppY 화면에 맞게 줄인 것이다. 원문에 있는 13개 중 이 앱에서 실제로
> 판단이 갈렸던 것만 남겼다.

**입력을 요구하기 전에, 그 입력이 무엇을 바꾸는지 먼저 보여준다.** 사용자는
채울 이유를 모르는 칸에서 이탈한다.

- 학교 찾기는 성적이 없어도 271개를 그대로 보여준다. 성적 칸은 화면을
  막지 않고 위쪽 배너로만 권한다("학점과 어학 점수를 입력해보세요 /
  지원 가능 여부를 알려드려요").
- 기본 정보를 저장하면 **홈이 아니라 학교 찾기로 보낸다.** 방금 채운 네 칸이
  무엇을 바꿨는지 그 자리에서 보이지 않으면, 사용자가 스스로 찾아가야 한다.

### 버튼은 다음에 올 화면을 예고한다

`다음` `확인` 같은 라벨은 누른 뒤에야 무슨 일이 생겼는지 알게 한다.

- 온보딩 제출 버튼: `저장하기` → **`내가 갈 수 있는 학교 보기`**.
  **라벨을 바꿨으면 목적지도 바꾼다** — 약속한 곳으로 보내지 않으면 라벨이
  거짓말이 된다(`saveAndLeave(writes, 'search.html')`).
- 홈의 입력 유도: `기본 정보를 입력해 주세요 →`.

### 못 하는 이유 대신 되는 조건을 적는다

- 지원 자격 판정: `GPA 3.4 이상 필요 · TOEFL 90 이상 필요` →
  **`GPA 3.4 · TOEFL 90부터 지원할 수 있어요`**. 숫자는 같지만 다음에 할 일이
  문장 안에 들어오고, 길이도 짧아진다.
- 배지 라벨(`기준 미달`)은 그대로 뒀다. 훑어보며 거르는 자리라 짧고 분명해야
  하고, 바로 아래 줄이 이미 되는 조건을 말해 준다.

### 오해가 생길 자리에는 한 줄을 붙인다

길게 설명하는 대신, 사용자가 실제로 착각하는 지점에만 한 줄을 둔다.

- 기록 탭: `🔒 나만 볼 수 있어요. 다른 사람에게 공개되지 않아요`.
  공유 기능이 아예 없다는 사실이 개인정보처리방침에만 적혀 있었다 — 정작
  글을 쓰는 화면에는 없었다.
- 예시값은 반드시 `예: ` 를 붙인다(`예: 김스텝`, `예: 3.62`). 접두사가 없으면
  자기 값이 이미 들어 있는 것으로 읽는 사람이 있다.

### 광고처럼 생긴 것을 두지 않는다

- 제휴 배너(보험·어학원·여행)는 누를 곳이 없는 더미 CTA였다. 출시 빌드에서는
  `release-flags.js` 가 통째로 걷어낸다. 배너가 하나 생기는 순간 그 근처
  카드까지 광고로 읽혀 눈에서 지워진다.

---

## 5. 첫 화면에 낯선 폼을 두지 않는다

가입 직후 무엇을 쓰는 앱인지 보기도 전에 입력칸부터 만나면, 채울 이유를 모르는
채로 채우게 된다. **서비스를 먼저 보여주고, 필요해진 자리에서 부른다.**

- 예전에는 가입하자마자 `onboarding.html` 로 튕겼다(학과·학점·어학·시기 네 칸).
  지금은 홈으로 들여보내고, 인사말 바로 아래 `기본 정보를 입력해 주세요 →`
  버튼 하나를 둔다. 누르면 같은 화면이 뜬다.
- **버튼을 보일지는 "물어봤는지"가 아니라 "값이 실제로 비었는지"로 정한다.**
  건너뛴 사람에게도, 그 화면이 생기기 전에 가입한 사람에게도 같은 안내가 떠야
  한다. ("물어봤다"를 기록하던 플래그는 지웠다 — 쓰는 곳이 없어졌다.)
- 채워지면 버튼은 스스로 사라진다. 할 일이 없어진 버튼을 남겨 두지 않는다.

---

## 6. 아직 안 되는 기능은 감춘다

App Store 심사(가이드라인 2.1)는 미완성 기능이 보이는 것을 리젝 사유로 삼는다.
"준비 중" 배지를 달아 두는 것도 마찬가지다.

- 코드를 지우지 않고 값 하나로 끈다 → [`js/release-flags.js`](js/release-flags.js)
- 정적 HTML은 `data-release="..."` 를 달면 꺼져 있을 때 통째로 사라진다
  (숨기지 않고 지운다 — `hidden` 은 화면 낭독기에 남는다)
- 지금 꺼진 것: `mentorStep`(후기 데이터 0건), `partnerPromo`(갈 곳 없는 CTA)

다만 **"데이터가 아직 없어요" 같은 빈 상태 문구는 미완성이 아니다.** 그건 남겨 둔다.

---

## 7. 흔히 밟는 지뢰

좁은 화면 + 유리 카드 조합에서 반복해서 걸린 것들.

### `backdrop-filter` 는 쌓임 맥락을 만든다

유리 카드(`backdrop-filter`) 위에 드롭다운을 띄우면 **다음 카드가 그 위를 덮는다.**
목록의 `z-index:50` 은 자기 카드 안에서만 유효하다.

→ 열려 있는 동안만 그 셀렉트를 품은 카드를 올렸다 내린다
  ([`js/components/searchable-select.js`](js/components/searchable-select.js))

### flex 컨테이너는 float 을 피한다

flex 는 BFC 라 위에 뜬 `float` 만큼 폭이 줄어든다. 제목 옆 버튼 자리를 float 으로
잡았더니 아래 배지 줄의 폭이 308px로 깎였다 → `clear: both`.

### 클래스로 준 `display` 는 `[hidden]` 을 이긴다

`.foo { display: flex }` 를 써 두면 `<div class="foo" hidden>` 이 안 접힌다.
→ `.foo[hidden] { display: none }` 을 같이 쓴다.

### 특이도 싸움을 하지 말고 구조를 바꾼다

`body[data-page="prepare"] .leg-card`(0,0,2,1)가
`.confirmed-card__departure .leg-card`(0,0,2,0)를 이겨서 배경만 지워지고
그림자·유리 효과가 남았다. 셀렉터를 더 세게 쓰는 대신,
**카드 껍데기를 아예 쓰지 않고 안쪽 조각만 가져다 썼다.**

### iOS 는 16px 미만 입력칸에서 화면을 확대한다

확대된 배율은 포커스를 잃어도 돌아오지 않아, 한 번 검색어를 치면 화면이 옆으로
밀린 채 남는다. 토큰이 15px이라 앱의 모든 입력칸이 여기 걸렸다.
→ `font-size: max(16px, var(--fs-body))`. `maximum-scale=1` 은 쓰지 않는다
  (손가락으로 키우는 것까지 막힌다).

### 위계가 같으면 크기도 같아야 한다

**역할이 같은 글자는 화면이 달라도 같은 크기·같은 무게여야 한다.** 한 화면
안에서는 안 보이지만, 화면을 옮겨 다니면 "여기만 왜 크지?" 로 티가 난다.
새 화면을 만들 때 크기를 새로 정하지 말고, **그 역할이 이미 어디에 있는지
먼저 찾아서 그 값을 쓴다.**

입력칸 위 라벨이 그랬다. 같은 역할인데 네 군데가 다 달랐다:

| 화면 | 전 | 후 |
| --- | --- | --- |
| 기본 정보 `.ob__label` | 13px / **800** | 13px / 700 |
| 계정 시트 `.field > span` | 13px / **400** | 13px / 700 |
| 학교 찾기 필터 `.filter-group__title` | 13px / 700 | 13px / 700 |
| 성적 폼 `.score-prompt__field span` | **11px** / 400 | 13px / 700 |

800 은 카드 제목(`.section-title h2` 16px/800)의 무게다. 라벨이 그 무게를 쓰면
제목과 경쟁한다. 400 은 본문 무게라 입력값과 구분이 안 된다. → **700**.

입력칸 안의 글자도 마찬가지다. 다른 화면은 전부 16px 인데 성적 폼만 13px
이었다(`--fs-small`). 크기가 튀는 것도 문제지만 **16px 미만은 iOS가 포커스할 때
화면을 확대해 버린다**(아래 지뢰 절) — 위계를 맞추니 그 버그도 같이 사라졌다.

steppY 의 현재 위계:

| 역할 | 값 |
| --- | --- |
| 카드 제목 | 16px / 800 |
| 화면 제목(앱바) | 15px / 800 |
| 화면 히어로 제목 | 20px(`1.25rem`) / 800 |
| 입력칸 위 라벨 | 13px / 700 |
| 본문·칩 | 13px / 400 |
| 입력칸 안 글자 | **16px** / 400 (아래로 내리지 않는다) |
| 보조 설명·메타 | 11px / 400 |

### 마크업이 다르면 크기가 갈린다

같은 층위의 카드 제목인데 하나는 `.info-panel h3`(13px), 하나는
`.section-title h2`(16px)였다. **카드가 나란히 쌓이는 화면에서는 제목을 하나로
맞춘다.**

---

## 8. 색과 대비

### 키 컬러는 "지금"에만 쓴다

앱 화면을 흰 바탕으로 정리하고 나니 브랜드가 랜딩에만 남고 앱 안에는 아무것도
없어 보였다. 그렇다고 하늘색(`--key-500`)을 여기저기 칠하면 어디를 눌러야 할지
알 수 없게 된다. **작고 반복되는 자리에만** 쓴다.

| 자리 | 예 |
| --- | --- |
| 말 거는 제목 `.brand-head` | "Your Step !", "To-Do", "Weather of Chapel Hill !", "How is … ?", "Cost of living, 얼마나 들까 ?" |
| 지금 와 있는 곳 | 탭바 현재 탭, 진행 단계의 발자국 자국·현재 점, 할 일 진행률 링, 파견 시기 계절 줄의 테두리 |
| 누를 수 있는 작은 것 | 할 일 `+` 버튼 |

**큰 색면에는 쓰지 않는다.** `.btn--accent`(학교 찾기·확정·적용·저장)를 하늘색으로,
형광 노랑(`#F7FF00`)으로도 칠해 봤는데 둘 다 화면에서 가장 큰 색면이라 그 색이
"브랜드"가 아니라 "버튼 색"으로 읽혔다. 버튼은 아이보리(`--amber-500`)로 되돌렸다.

**선택 상태에도 쓰지 않는다.** 칩·순위 칩·모드 토글·스위치를 칠해 봤는데, 칩은 한
화면에 열 몇 개가 깔리고 몇 개든 동시에 켜지는 것이라 색이 흩어지면 뜻이 사라지고
그냥 알록달록해진다. 선택 상태는 `--sky-500` / `--amber-500` 그대로.

지키는 규칙:

- **로그인·회원가입 계열 버튼은 계속 남색(`.btn--primary`).** 인증은 "다음 걸음"이
  아니라 문턱이고, 랜딩·인증 화면의 남색 톤을 앱 안까지 잇는 역할을 한다.
- **상태 색(민트 `--mint-500` 지원 가능 / 코랄 `--coral-500` 불가)은 건드리지 않는다.**
  키 컬러가 "브랜드", 상태 색이 "데이터가 말하는 것"으로 갈려야 한다.
- **키 컬러 위 흰 글자(1.6:1)는 금지**, 글자는 항상 `--key-ink`(= `--ink-900`).

### 하늘색을 글자로 쓸 때 — 색을 내리지 말고 테두리를 두른다

흰 바탕 위 `#12E5E2` 는 1.6:1 이다. 두 가지 길이 있었다.

1. 색을 어둡게 내린다 (`#00B2B0`, 2.6:1) — 읽히지만 이 색 특유의 밝기가 사라진다.
2. 밝은 색 그대로 두고 네이비 테두리를 한 겹 두른다 — 랜딩 워드마크와 같은 처리.

2번을 골랐다. 대신 **머리카락 두께(화면에서 약 0.3px)** 여야 한다. 처음에 두 배로
줬더니 글자가 하늘색이 아니라 남색으로 보였다.

한 줄 안에 로고 글씨체 SVG와 Pretendard 글자가 섞이므로 굵기를 **두 군데** 맞춰야
한다. 그리고 두 값은 같지 않다 — `-webkit-text-stroke` 는 획 가운데에 걸려 바깥으로
나오는 게 절반이고, SVG `stroke-width` 는 `scale(0.1)` 안이라 적은 값의 1/10 이
사용자 좌표 두께다. 물음표만 테두리가 없어 보이던 게 이 차이를 놓쳐서였다.

    .brand-head  -webkit-text-stroke: 0.028em   (22px 기준 바깥 0.31px)
    .wm-text     stroke-width: 240              (22px 기준 바깥 0.30px)

둘 다 `paint-order: stroke fill` 이 필요하다. 없으면 테두리가 획 가운데로 파고들어
글자가 도리어 얇아진다.

### 로고 글씨체는 폰트가 아니라 패스다

`assets/wordmark-glyphs.json` 에 A–Z, a–z, `!` 만 있다. 한글도 물음표도 쉼표도 없다.
[js/wordmark-text.js](js/wordmark-text.js) 가 `[data-wordmark]` 안에서 **알파벳이
이어지는 구간만** 잘라 SVG 로 갈아 끼우고 나머지는 Pretendard 로 남긴다.

여기서 나온 문구 규칙:

- **제목에 한글 고유명사를 섞지 않는다.** "Weather of 미국 · Chapel Hill!" 은 국가명이
  본문 글씨체로 튀어나와 한 줄이 두 글씨체로 쪼개졌다. 지금은 영문 지명만 쓴다
  (`schoolPlaceEn()` — `campusCity` 우선, 괄호·한자·분음 부호를 털어 낸 아스키).
- **`!` `?` 앞은 한 칸 띄운다.** 로고 글씨체의 `!` 는 앞 글자에 닿고, `?` 는 Pretendard
  라 붙이면 글씨체 경계가 그대로 드러난다. 한 칸이 그 경계를 가린다.

죽은 규칙을 남겨 두면 색을 여기에 칠했다고 착각한다 — `.journey-card*` 가 그랬다.
마크업이 `.journey__*` 로 바뀐 뒤에도 CSS가 남아 있어서, 거기에 키 컬러를 넣고
화면에서 못 찾았다. **색을 바꾸기 전에 그 클래스가 지금 쓰이는지 먼저 grep 한다.**

### 배경이 고르지 않으면 대비를 계산한다

배경이 사진이나 그라데이션이면 글자 색을 눈으로 고르지 말고 대비를 따진다.

- 인트로 그라데이션이 바닥에서 하늘색(#7CA2C6)까지 밝아지는데 그 위 글자는 전부
  크림색이었다 → 대비 2.6:1. 가장 밝은 지점을 80% 부근으로 옮기고 바닥을 중간
  남색으로 내려 5.9:1로 올렸다.
- 유리 카드 위 크림색 라벨이 묻히면 카드를 없애는 것도 방법이다(인트로 숫자).
- 사진 위 글자에는 그림자를 한 겹 깐다. 사진은 자리마다 밝기가 다르다.
- 홈 배경 사진은 좌우를 뒤집어 어두운 날개를 인사말 반대편으로 보냈다 —
  **글자를 옮기는 대신 배경을 옮기는 선택지도 있다.**

---

## 9. 고치기 전에 재고, 고치고 나서 확인한다

- 화면을 눈으로만 보고 여백을 조정하지 않는다.
  `getBoundingClientRect()` 로 실제 간격·줄 수를 재고 숫자로 확인한다.
- 글자 크기를 정할 때는 후보를 훑어 재 본다. 요소에 `style.fontSize` 를 바꿔
  넣으며 `height / lineHeight` 로 줄 수를 세면, 몇 px 부터 줄이 늘어나는지
  한 번에 나온다 — 짐작으로 한 단계씩 낮추는 것보다 빠르고 정확하다.
- 브라우저 패널이 닫혀 있으면 페이지가 렌더링되지 않아 스크린샷이 낡은 화면을
  준다. 그럴 때는 `read_page` / JS 측정으로 확인한다.
- 데이터가 얽힌 화면은 **실제 데이터로** 연다. 목업으로 보면 "홍콩 · 香港 Hong
  Kong" 같은 중복이 안 보인다.

---

## 파일 지도

| 무엇 | 어디 |
| --- | --- |
| 색·간격·서체 토큰 | [`css/tokens.css`](css/tokens.css) |
| 데스크탑 기준 컴포넌트 | [`css/components.css`](css/components.css) |
| 앱(402px) 스킨 — 위를 덮어씀 | [`css/app.css`](css/app.css) |
| 화면별 | [`css/pages/`](css/pages/) |
| 미완성 기능 스위치 | [`js/release-flags.js`](js/release-flags.js) |
| 학교 상세 모달 | [`js/components/school-modal.js`](js/components/school-modal.js) |
| 생활 점수 카드 | [`js/components/radar-chart.js`](js/components/radar-chart.js) |

`css/app.css` 가 `components.css` 보다 뒤에 실린다. 앱에서만 다르게 보여야 하는
것은 app.css 에 쓰고, **양쪽에 같은 속성이 있으면 app.css 쪽을 고친다.**
