# steppY UI 원칙

402 × 874 한 화면에서 손가락으로 읽는 앱이다. 여기 적힌 것은 취향이 아니라
그 조건에서 반복해서 부딪힌 문제와, 그때마다 내린 결정이다. 새 화면을 만들거나
기존 화면을 고칠 때 이 기준으로 판단한다.

---

## 1. 세로 길이가 가장 비싸다

좁은 화면에서 세로로 길어지면 사용자는 **읽지 않고 스크롤**한다. 그래서 길이를
늘리는 모든 것 — 줄바꿈, 카드, 여백, 설명문 — 은 값을 치르고 들어와야 한다.

### 줄바꿈부터 없앤다

한 줄에 들어갈 수 있는 것이 두 줄이 되면, 화면에 담기는 정보가 절반으로 준다.

- **제목이 접히면 폭을 넓힐 자리를 먼저 찾는다.** 학교 상세의 긴 이름이 석 줄로
  접혔을 때, 글자 크기를 줄이는 대신 오른쪽 버튼(닫기·찜하기)을 흐름 밖으로 빼고
  `float` 한 칸만 남겼다. 첫 줄만 버튼을 비켜 가고 둘째 줄부터 폭을 다 쓴다 →
  석 줄이 두 줄이 됐다.
- **같은 층위의 값은 한 줄에 둔다.** `지원 가능 / 모집 / GPA 컷 / TOEFL 컷` 이
  두 줄로 나뉘면 앞 두 개만 읽고 만다. 배지 좌우 여백(16 → 9px)과 간격(10 → 6px)을
  줄여 넷을 한 줄에 넣었다. 그래도 넘치면 접지 말고 가로로 밀어서 본다
  (`flex-wrap: nowrap; overflow-x: auto`).
- **문장형 제목은 크기를 재서 정한다.** 데스크탑 기준 32px(`--fs-h1`)로 쓴
  랜딩 제목이 402px 화면에서 네 줄로 접혔다. `<br>` 로 의도한 자리 말고 엉뚱한
  데서 끊기면 문장이 아니라 단어 뭉치로 읽힌다.
  → **쓴 대로 들어가는 가장 큰 크기를 실제로 재서 고른다.**
  "교환학생 준비부터 추억 정리까지, / 교환학생의 모든 STEP을 도와줍니다" 는
  340px 폭에서 24px→2줄, 25px→3줄이라 24px(`1.5rem`)로 정했다.
  짧은 제목("Four Steps!")은 같은 크기로 한 줄이니 크기를 나눌 필요가 없다.
  **문구를 고치면 줄 수가 달라지니 다시 잰다.**
- **폭부터 확인하고 크기를 건드린다.** 위 제목이 접힌 진짜 이유는 글자가 커서가
  아니라 좌우 여백이 62px씩이라 본문 폭이 278px밖에 안 됐기 때문이다.
  앱 화면 기준(31px)으로 좁히니 340px이 되면서 접힘이 절반으로 줄었다.
- **긴 데이터를 넣기 전에 칸을 세어 본다.** 날씨 줄에 `비 오는 날 월 13일 · 67mm`
  를 넣었더니 한 줄이 69px로 부풀었다. `☔ 13일 · 67mm` 로 줄이고 "비 오는 날 월"
  이라는 설명은 목록 아래 캡션 한 줄로 옮겨서 52px이 됐다.

### 카드는 내용이 상자를 요구할 때만

껍데기(배경·테두리·그림자·안쪽 여백)는 그 자체로 세로 길이를 먹는다.
**내용보다 껍데기가 크면 껍데기를 벗긴다.**

- 인트로의 "네 걸음"에서 흰 카드를 걷어냈다. 네 개의 상자가 아니라 이어지는
  하나의 흐름이라, 카드로 끊으면 순서가 안 읽힌다. 여백과 번호로만 나눈다.
- 국제처 링크는 링크 한 줄이 전부였다. 카드를 지우고 한 줄로 뒀다.
- 확정 학교 카드 안의 출국 D-day도 카드였다. 카드 안에 카드가 들어앉아 "잘린
  상자"로 보여서, 껍데기만 벗기고 경로 그림은 남겼다(182 → 62px).
- 인트로 숫자(271/76/32)의 유리 카드도 없앴다. 가는 세로선으로만 나눈다.

### 여백은 두 번 겹치기 쉽다

컨테이너의 `gap` 과 자식의 `margin` 이 더해지는 실수가 반복됐다.

- 생활 점수의 근거 한 줄: `.score-rows` 의 `gap:16px` 위에 `margin-bottom:16px`
  이 얹혀 32px이었다. 아래 여백을 빼서 16px로 줄이고, 위는 당겨서(-8px) 자기
  지표에 붙였다. **설명은 설명하는 대상에 붙어 있어야 한다.**
- 여백을 고칠 때는 눈으로 보지 말고 `getBoundingClientRect()` 로 실제 간격을 잰다.

### 접을 수 있으면 접는다

- 생활 준비 4개(보험·장학금·통신사·계좌)는 제목만 세우고 눌러야 열린다.
  보험을 알아보는 날과 계좌를 여는 날은 다르다.
- 이미 체크리스트가 같은 방식이라 조작이 낯설지 않다 — **새 상호작용을 만들기
  전에 이미 쓰는 방식이 있는지 본다.**

---

## 2. 문구 다이어트

한국어 UI는 조사와 서술어로 금방 길어진다. **뜻이 유지되는 한 짧은 쪽**을 쓴다.

### 없애도 되는 단어를 찾는다

| 전 | 후 | 이유 |
| --- | --- | --- |
| 오늘의 할 일 | 할 일 | 오늘 것만 있는 게 아니다 |
| ② 날씨 · 생활 정보 | Weather of 미국 · Bellingham! | 번호와 분류어 대신 그 학교 이야기 |
| ⑤ 공식 링크 | 국제처 링크 | "공식"은 링크면 당연하다 |
| D-346 출국까지 | D-346 | 서울 → Liverpool 이 바로 위에 있다 |
| 비 오는 날 월 13일 | ☔ 13일 | 기호가 단어를 대신한다 |

단위는 숫자보다 작게 둔다. `271개` 에서 숫자는 36px, `개` 는 라벨과 같은 11px —
읽어야 하는 것은 숫자지 단위가 아니다.

### 같은 말을 두 번 하지 않는다

- 지도 카드 제목이 `미국 · Bellingham` 인데 그 아래 캡션도 같은 말이었다 → 캡션 삭제.
- 날씨의 계절별 줄이 "서울보다 2°C 추움"을 계산해 주는데, DB의
  `korea_comparison` 도 "봄 서울보다 2도 따뜻" 이었다 → 같은 형태면 감춘다.
- `city` 칸에 나라 이름이 들어 있는 행이 있다(홍콩 `香港 Hong Kong`) →
  "홍콩 · 香港 Hong Kong" 이 되지 않게 도시가 나라를 품고 있으면 나라만 남긴다.
- 서울과의 강수 비교를 계절마다 붙이면 네 번 반복된다 → 목록 아래 한 줄로 모은다.

### 설명이 필요하면, 설명이 필요 없게 고칠 수 있는지 먼저 본다

`막대는 파견교 271곳끼리 견준 상대 위치예요. 그 아래 한 줄이 그 판단의 근거가 된
실제 수치고요.` — 이 문장은 읽고 나서도 무슨 말인지 모르겠다는 말을 들었다.

→ `막대가 길수록 다른 파견교보다 좋다는 뜻이에요. 막대 아래 한 줄은 그 막대가
나온 실제 숫자고요.`

**추상어("상대 위치", "판단의 근거")를 동사와 비교로 바꾼다.**

### 말투

- 존댓말 `-요` 체. `-습니다` 는 딱딱하다.
- 화면 제목은 가볍게 말을 걸어도 된다: `How is 미국 · Bellingham?`,
  `Cost of living, 얼마나 들까?`
- 다만 **되돌릴 수 없는 동작은 가볍게 쓰지 않는다.** 회원 탈퇴는
  "계정을 지우면 되돌릴 수 없어요" 로 시작하고 무엇이 사라지는지 항목으로 적는다.

---

## 3. 숫자는 해석까지 준다

0~100 점수만 던지면 그 동네가 어떤 곳인지 알 수 없다. **점수 아래에 그 점수를
만든 실제 수치를 붙인다.**

```
🚌 교통 이동성  ████████░░  매우 우수
   도심까지 대중교통 36분 · 1km 안 정류장 50곳
```

- 비교 기준이 있으면 같이 준다. 날씨는 서울 평년값(기상청 1991~2020) 대비 차이로
  적고, 강수는 서울의 계절별 강수일수를 아래에 한 줄로 둔다.
- **기준이 없으면 만들어 내지 않는다.** 치안·상권 점수는 데이터셋에 서울이 없어
  서울과 견줄 수 없다. 그래서 271곳 안에서의 상대 위치로만 말한다.
- 근거가 없는 항목은 "자료 없음" 으로 비워 둔다. 반쪽짜리 숫자보다 낫다.

### 측정하지 않은 것을 단정하지 않는다

기후 필터 라벨이 `온화·잦은 비`, `고온다습`, `한랭·일조 짧음` 이었는데, 분류는
계절 평균기온 네 개로만 했고 강수·습도·일조 자료가 아예 없었다. 마드리드(여름
14mm·3일)와 리버풀(여름 92mm·17일)이 같은 칸에 들어가 있었다.

→ `겨울 온화` / `연중 더움` / `겨울 영하` 로 바꾸고, 강수는 실제로 수집해서
   따로 보여준다.

---

## 4. 아직 안 되는 기능은 감춘다

App Store 심사(가이드라인 2.1)는 미완성 기능이 보이는 것을 리젝 사유로 삼는다.
"준비 중" 배지를 달아 두는 것도 마찬가지다.

- 코드를 지우지 않고 값 하나로 끈다 → [`js/release-flags.js`](js/release-flags.js)
- 정적 HTML은 `data-release="..."` 를 달면 꺼져 있을 때 통째로 사라진다
  (숨기지 않고 지운다 — `hidden` 은 화면 낭독기에 남는다)
- 지금 꺼진 것: `mentorStep`(후기 데이터 0건), `partnerPromo`(갈 곳 없는 CTA)

다만 **"데이터가 아직 없어요" 같은 빈 상태 문구는 미완성이 아니다.** 그건 남겨 둔다.

---

## 5. 흔히 밟는 지뢰

좁은 화면 + 유리 카드 조합에서 반복해서 걸린 것들.

### `backdrop-filter` 는 쌓임 맥락을 만든다

유리 카드(`backdrop-filter`) 위에 드롭다운을 띄우면 **다음 카드가 그 위를 덮는다.**
목록의 `z-index:50` 은 자기 카드 안에서만 유효하다.

→ 열려 있는 동안만 그 셀렉트를 품은 카드를 올렸다 내린다
  ([`js/components/searchable-select.js`](js/components/searchable-select.js))

### flex 컨테이너는 float 을 피한다

flex 는 BFC 라 위에 뜬 `float` 만큼 폭이 줄어든다. 제목 옆 버튼 자리를 float 으로
잡았더니 아래 배지 줄의 폭이 308px로 깎였다 → `clear: both`.

### 클래스로 준 `display` 는 `[hidden]` 을 이긴다

`.foo { display: flex }` 를 써 두면 `<div class="foo" hidden>` 이 안 접힌다.
→ `.foo[hidden] { display: none }` 을 같이 쓴다.

### 특이도 싸움을 하지 말고 구조를 바꾼다

`body[data-page="prepare"] .leg-card`(0,0,2,1)가
`.confirmed-card__departure .leg-card`(0,0,2,0)를 이겨서 배경만 지워지고
그림자·유리 효과가 남았다. 셀렉터를 더 세게 쓰는 대신,
**카드 껍데기를 아예 쓰지 않고 안쪽 조각만 가져다 썼다.**

### iOS 는 16px 미만 입력칸에서 화면을 확대한다

확대된 배율은 포커스를 잃어도 돌아오지 않아, 한 번 검색어를 치면 화면이 옆으로
밀린 채 남는다. 토큰이 15px이라 앱의 모든 입력칸이 여기 걸렸다.
→ `font-size: max(16px, var(--fs-body))`. `maximum-scale=1` 은 쓰지 않는다
  (손가락으로 키우는 것까지 막힌다).

### 마크업이 다르면 크기가 갈린다

같은 층위의 카드 제목인데 하나는 `.info-panel h3`(13px), 하나는
`.section-title h2`(16px)였다. **카드가 나란히 쌓이는 화면에서는 제목을 하나로
맞춘다.**

---

## 6. 색과 대비

배경이 사진이나 그라데이션이면 글자 색을 눈으로 고르지 말고 대비를 따진다.

- 인트로 그라데이션이 바닥에서 하늘색(#7CA2C6)까지 밝아지는데 그 위 글자는 전부
  크림색이었다 → 대비 2.6:1. 가장 밝은 지점을 80% 부근으로 옮기고 바닥을 중간
  남색으로 내려 5.9:1로 올렸다.
- 유리 카드 위 크림색 라벨이 묻히면 카드를 없애는 것도 방법이다(인트로 숫자).
- 사진 위 글자에는 그림자를 한 겹 깐다. 사진은 자리마다 밝기가 다르다.
- 홈 배경 사진은 좌우를 뒤집어 어두운 날개를 인사말 반대편으로 보냈다 —
  **글자를 옮기는 대신 배경을 옮기는 선택지도 있다.**

---

## 7. 고치기 전에 재고, 고치고 나서 확인한다

- 화면을 눈으로만 보고 여백을 조정하지 않는다.
  `getBoundingClientRect()` 로 실제 간격·줄 수를 재고 숫자로 확인한다.
- 글자 크기를 정할 때는 후보를 훑어 재 본다. 요소에 `style.fontSize` 를 바꿔
  넣으며 `height / lineHeight` 로 줄 수를 세면, 몇 px 부터 줄이 늘어나는지
  한 번에 나온다 — 짐작으로 한 단계씩 낮추는 것보다 빠르고 정확하다.
- 브라우저 패널이 닫혀 있으면 페이지가 렌더링되지 않아 스크린샷이 낡은 화면을
  준다. 그럴 때는 `read_page` / JS 측정으로 확인한다.
- 데이터가 얽힌 화면은 **실제 데이터로** 연다. 목업으로 보면 "홍콩 · 香港 Hong
  Kong" 같은 중복이 안 보인다.

---

## 파일 지도

| 무엇 | 어디 |
| --- | --- |
| 색·간격·서체 토큰 | [`css/tokens.css`](css/tokens.css) |
| 데스크탑 기준 컴포넌트 | [`css/components.css`](css/components.css) |
| 앱(402px) 스킨 — 위를 덮어씀 | [`css/app.css`](css/app.css) |
| 화면별 | [`css/pages/`](css/pages/) |
| 미완성 기능 스위치 | [`js/release-flags.js`](js/release-flags.js) |
| 학교 상세 모달 | [`js/components/school-modal.js`](js/components/school-modal.js) |
| 생활 점수 카드 | [`js/components/radar-chart.js`](js/components/radar-chart.js) |

`css/app.css` 가 `components.css` 보다 뒤에 실린다. 앱에서만 다르게 보여야 하는
것은 app.css 에 쓰고, **양쪽에 같은 속성이 있으면 app.css 쪽을 고친다.**
