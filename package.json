{
  "name": "steppy",
  "version": "1.0.0",
  "private": true,
  "description": "steppY — 교환학생 준비 플랫폼",
  "scripts": {
    "build": "node tools/build-www.mjs",
    "sync": "npm run build && npx cap sync",
    "ios": "npm run sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/cli": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/ios": "^8.5.2"
  }
}
